extends layouts/layout.pug

mixin entry(url, title, date, categories, thumb)
        a(href=url)
            .frame
                img(src=thumb, alt=title)
            p.work= title
            p #{date} &#8212; #{categories}

block text
    .fit.pads
        h1 i’m a weird cat that plays around with photoshop one day and kinda likes it so i run with it and oof i have to work now.
block overflow
    .grid
        each work in collections.work
            +entry(work.url, work.data.info.title, work.data.info.date, work.data.info.categories, work.data.info.thumb)
